#include <stdio.h>
/**
 * main -  a C program that prints and  compile using -Wall gcc option.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
